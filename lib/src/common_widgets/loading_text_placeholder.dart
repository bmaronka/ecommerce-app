import 'package:flutter/material.dart';

class LoadingTextPlaceholder extends StatelessWidget {
  const LoadingTextPlaceholder({super.key});

  @override
  Widget build(BuildContext context) => Container(
        color: Colors.white,
        height: 10,
        width: double.infinity,
      );
}
